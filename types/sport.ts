export type ApparelType = {
    id: number;
    name: string;
    desc: string;
    image: string;
}

export type InstagramProductType = {
    id: number;
    title: string;
    image: string;
    link: string;
}
