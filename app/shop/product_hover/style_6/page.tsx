import { StyleSixView } from '@/views';
import { Metadata } from 'next';

export const metadata: Metadata = {
    title: "Shop Layout Styles",
};

export default function page() {
    return <StyleSixView />
}
